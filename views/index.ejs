<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
</head>
<body>
  <%- include ('layout') %>
  <div class="container">
    <% if (projects.length>0) { %>
    <div class="row">
    <% projects.forEach(function(project){ %>
        <div class="col-lg-4 col-md-6 mb-4">
          <div class="card h-100">
            <a href="#"></a><img class="card-img-top" src=" <%- project.image %> " alt="">
            <div class="card-body">
              <h4 class="card-title">
                ชื่อ: <a href="#"> <%= project.name %> </a>
              </h4>
              <h6>วันสร้างโปรเจค : <%= project.date %> </h6>
              <p class="card-text"> <%= project.description %> </p>
            </div>
            <div class="card-footer">
              <small class="text-muted">
                <a href="#">See More</a>
              </small>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
    <% } else { %> 
      <h2>ไม่มีข้อมูลในฐานข้อมูล</h2>
    <% } %>
  </div>
  <footer class="py-5 bg-dark">
    <div class="container">
      <p class="m-0 text-center text-white">Project Management : <a href="/admin/projects">Backend</a></p>
    </div>
  </footer>
</body>
</html>